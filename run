#!/bin/bash
export PYTHONPATH=.
echo "🚀 Starting Sovereign SMC Scanner (Simple Mode)..."
# Kill any existing instances first
pkill -9 -f "python src/runners/local_scanner.py" || true
rm -f /tmp/smc_scanner.lock
venv/bin/python src/runners/local_scanner.py
